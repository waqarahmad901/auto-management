@{
    ViewBag.Title = "Play";
}

<div id="bodyContainer" >
    <div ng-controller="ResourceController">
        <div>{{Error}}</div>
        <div class="row">
            <div class="col-sm-8 form-horizontal">
                <div style="margin-top:10px"></div>
                @*<div class="html"></div>
    <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
            <input type="button" value="Save" class="btn-default" ng-click="saveStudent(student)" />
        </div>
    </div>*@
            </div>
        </div>
    </div>
    <!-- Modal Button-->
    <span class="item-delete-button">
        <button class="btn btn-default col-lg-3 col-lg-offset-3" data-toggle="modal" data-target="#abc" onclick="deleteStart(this)">
            Add New Student
        </button>
    </span>
</div>

<div class="modal fade" id="abc" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content" ng-controller="ResourceController">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title" id="myModalLabel">Add New Student</h4>
            </div>
            <div class="modal-body form-horizontal">
                <div class="html"></div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                <button type="button" onclick="saveEntity(entity)" class="btn btn-primary">Save</button>
            </div>
        </div>
    </div>
</div>

